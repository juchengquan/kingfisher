---
name: probe-skill
description: A skill that exists so seeding has a folder to copy.
---

# Probe skill

A skill is a folder rather than a file, which is the one thing about the format
the seeding tests need to be true. Nothing here is advice.
